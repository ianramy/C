#include <stdio.h>

// Q1: Can we use keywords as identifiers?
int main()
{
    // int int = 5;  // invalid combination of type specifiers ('int' is a keyword)
    int myInt = 5;
    printf("Q1 Answer: myInt = %d\n", myInt);
    return 0;
}
