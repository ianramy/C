#include <stdio.h>

// Q2: Are identifiers case-sensitive?
int main()
{
    int value = 10;
    int Value = 20; // Different identifier
    printf("Q2 Answer: value = %d, Value = %d\n", value, Value);
    return 0;
}
